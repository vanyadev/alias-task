export const DEFAULT_WORDS_TO_WIN = 60;

export const DEFAULT_PENALTY_SETTING = false;

export const DEFAULT_ROUND_TIME = 60;

export const DEFAULT_ROUND_COUNTER = 0;

export const DEFAULT_GAME_COUNTER = 0;
